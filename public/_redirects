# Netlify / Cloudflare Pages redirects
# Trailing-slash canonical (Astro emits directory-format URLs).

# WWW → apex canonical (uncomment when the domain is live)
# https://www.epargnermaroc.ma/*  https://epargnermaroc.ma/:splat  301!

# Legacy paths (add as content evolves)
# /old-slug/  /articles/new-slug/  301
